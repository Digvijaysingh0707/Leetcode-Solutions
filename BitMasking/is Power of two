class Solution {
    public boolean isPowerOfTwo(int n) {
       int bits=0;
        while(n>0){
            n=(n&(n-1));
            bits++;
        }
        if(bits==1){
            return true;
        }
        else return false;
        
    }
}
