import java.util.*;

public class FrequentElements {

    static boolean contains(int a[],int k){
        HashMap<Integer,Integer> map = new HashMap<>();
        for (int i=0;i<a.length;i++){
            int current=a[i];
            if ((map.containsKey(current))&&map.get(current)<=k){
            //map.get prints index of current
                return true;
            }
            map.put(current,i);
        }
        return false;
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n=sc.nextInt();
        int a[]= new int[n];
        for (int i=0;i<n;i++){
            a[i]=sc.nextInt();
        }
        int k=sc.nextInt();
        if (contains(a,k)){
            System.out.println("true");
        }
        else{
            System.out.println("false");
        }
    }
}

